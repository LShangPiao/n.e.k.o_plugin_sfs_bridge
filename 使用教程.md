# 和猫娘一起玩《航天模拟器》

通过 N.E.K.O 让猫娘住进 Spaceflight Simulator，陪你造火箭、送它上天。

---

## 简单来说

1. 把 `SFS-Agent.dll` 放进游戏的 `Mods\SFS-Agent\`，**重启游戏**
2. 在 N.E.K.O 插件面板点刷新，启用「和猫娘一起造火箭(SFS)」
3. 对猫娘说一句话，比如「**下一次火箭发射是什么时候**」

连接正常的话，浏览器会自动打开一个面板（<http://127.0.0.1:21578/>），
上面能看到实时状态。

> **顺序不能反。** 游戏必须先启动。插件开早了没关系，它会每 5 秒自动重连。

---

## 一、环境准备

| 依赖 | 版本 |
| --- | --- |
| Spaceflight Simulator | 1.5.9.8 及以上（实测 1.6.00.16） |
| SFS-Agent 模组 | v0.3.0+ |
| N.E.K.O | 最新版 |

---

## 二、安装

### 1. 装游戏模组

从 [Releases](https://github.com/LShangPiao/SFS-Agent/releases) 下载 `SFS-Agent.dll`，
放到：

```
<Steam>\steamapps\common\Spaceflight Simulator\Spaceflight Simulator Game\Mods\SFS-Agent\SFS-Agent.dll
```

⚠️ **必须是 `Mods\SFS-Agent\` 这个子目录**，文件名和目录名一致。
不要平铺到 `Mods\` 根目录 —— SFS 是一目录一模组，放错位置会加载不到。

**然后重启游戏。** 模组只在游戏启动时加载。

装好后可以验证一下：浏览器打开 <http://127.0.0.1:21578/ping>，应该看到
`{"ok":true,"mod":"sfs_agent",...}`。

### 2. 装插件

在 N.E.K.O 的插件中心搜索「和猫娘一起造火箭」，或直接导入
`sfs_bridge-0.1.0.neko-plugin`，然后在插件面板启用。

### 3. 就绪检查

游戏启动后，模组会**自动打开浏览器面板**。面板顶部如果是绿色的「已连接」，
就说明一切正常。

不想让它自动开？在 `Mods\SFS-Agent\sfs-agent.ini` 里把 `open_browser` 改成 `false`。

---

## 三、日常使用

所有交互都用自然语言，不需要记指令。

### 发射相关

| 你想做什么 | 可以这样说 |
| --- | --- |
| 问下一次发射 | 「下一次火箭发射是什么时候」「最近有什么发射」 |
| 看未来任务 | 「接下来有哪些发射任务」「下个月有什么发射」 |
| 按条件筛 | 「SpaceX 下次什么时候发」「从卡纳维拉尔发的有哪些」 |

### 查航天知识

| 你想做什么 | 可以这样说 |
| --- | --- |
| 查火箭 | 「猎鹰九号是什么火箭」「长征五号的参数」 |
| 查航天器 | 「天舟飞船是什么」「国际空间站的资料」 |
| 查机构 | 「NASA 是干什么的」「CNSA 有哪些任务」 |
| 查人物 | 「有哪些中国航天员」 |
| 查技术文献 | 「离子推进器的研究资料」「NASA 有关月壤的论文」 |
| 随便问点啥 | 「土星五号的推力是多少」 |

> 猫娘会依次查 **发射数据库 → 中文百科 → NASA 文献 → 网页搜索**。
> **查不到她会直说查不到**，不会编。详见第七节。

### 造火箭

| 你想做什么 | 可以这样说 |
| --- | --- |
| 看看存过什么 | 「我存过哪些火箭」「我有哪些设计」 |
| 载入现成的 | 「载入我那个玄鸟R」「把我存的某枚火箭放上去」 |
| 加零件 | 「在火箭下面加个引擎」「再加一节燃料罐」 |
| 问零件 | 「有哪些零件可以用」 |
| 评审设计 | 「你看我这火箭怎么样」「这设计有什么问题」 |

> **造火箭优先用「载入蓝图」**。手拼单个零件需要算对贴合间距，不如直接加载
> 你自己存过的设计可靠。详见第六节。

### 飞行控制

| 你想做什么 | 可以这样说 |
| --- | --- |
| 点火 | 「点火」「启动引擎」 |
| 油门 | 「油门开到 80%」「油门全开」「关油门」 |
| 分级 | 「分离一级」「执行下一级」 |
| 转向 | 「向左转」「往右偏一点」 |
| RCS | 「打开 RCS」「关掉 RCS」 |
| 读仪表 | 「我现在飞多高」「速度多少」「还剩多少燃料」 |

### 看画面

| 你想做什么 | 可以这样说 |
| --- | --- |
| 看现在的画面 | 「你看我造的火箭」「看看我现在飞到哪了」 |
| 看整体 | 「退远一点看」「拉近一点」 |

### 操作界面

| 你想做什么 | 可以这样说 |
| --- | --- |
| 开始游戏 | 「点开始游戏」「进我的存档」 |
| 逛菜单 | 「打开设置」「返回主菜单」 |

---

## 四、她不会抢你的鼠标

这是这个插件最特别的地方。

**她点击界面的时候，你的鼠标不会动，游戏窗口也不会被切到前台。**
你可以一边用浏览器查资料，一边让她在后台造火箭。

原理是：点击走游戏自己的输入派发接口，按键靠拦截游戏内部的按键查询 ——
两者都不碰系统输入。

| 操作 | 会抢鼠标吗 | 游戏要在前台吗 |
| --- | --- | --- |
| 点界面按钮 | ❌ 不会 | ❌ 不用 |
| 发按键 | ❌ 不会 | ❌ 不用 |
| 飞行控制指令 | ❌ 不会 | ❌ 不用 |
| 视角控制 | ❌ 不会 | ❌ 不用 |

---

## 五、独占模式

如果你希望**她玩的时候你别插手**（避免误触打断她操作），可以开独占模式。

开启后：

- 你的鼠标点击和键盘按键**会被游戏忽略**，只有她能操作
- 屏幕上下会出现**淡蓝色渐变**和「Agent 操作中」
- 想解除：在浏览器面板上把开关关掉，或按 **F10** 应急

**怎么开**：打开 <http://127.0.0.1:21578/>，找到「Agent 独占模式」卡片，
点一下按钮即可，**不用重启游戏**。

> 游戏画面里**故意不放解除按钮** —— 免得你在独占状态下还要满屏幕找按钮。

---

## 六、关于造火箭

### 为什么优先「载入蓝图」

游戏里的火箭设计存在 `Saving/Blueprints/<名字>/Blueprint.txt`，里面每个零件都带：

- **位置和朝向**
- **尺寸与缩放**（燃料罐多宽多高）
- **纹理**
- **它属于第几级**

插件是把**整份蓝图交给游戏自己解析的**，所以尺寸、分级全都正确。

实测加载一枚 141 个零件的火箭：质量 712.4 吨、推力 356 吨、推重比 0.51 ——
和蓝图文件里的数据完全一致。

### 手拼单个零件为什么容易出问题

零件的坐标是**贴合点**，间距由零件自身高度决定。不同零件的高度变量名还不统一
（`height` / `width` / `size` / `height_max`，有的零件干脆没有）。

实测按固定间距拼出来的火箭，发射时上面的零件会掉下来把火箭砸爆。

所以除非你只是想在已有设计上补一两个零件，否则**都建议直接用蓝图**。

### 怎么存蓝图

在游戏建造界面点右上角的 `Save` 即可。之后猫娘就能通过「我存过哪些火箭」看到它。

---

## 七、她不会瞎编

航天数据最怕编 —— 编出来的推力、尺寸、服役时间看着都很像真的，但会误导人。

所以插件在查不到的时候会**明确说查不到**，而不是让模型自由发挥：

| 情况 | 她的表现 |
| --- | --- |
| 数据库里没有这个条目 | 直说「我这边没有查到」，不猜型号尺寸 |
| 搜索结果不相关 | 丢弃这些结果，不当成答案 |
| 接口超时/失败 | 如实报错，不用旧数据冒充 |
| 游戏没运行 | 直说连不上游戏，不编造飞行数据 |
| 画面读不到 | 直说画面不可用，不猜测画面内容 |

---

## 八、常见问题

**Q：插件显示连不上游戏？**

按顺序检查：
1. 游戏**正在运行**吗？（模组只在启动时加载，装完必须重启游戏）
2. `Mods\SFS-Agent\SFS-Agent.dll` 位置对吗？（必须是这个子目录）
3. 浏览器打开 <http://127.0.0.1:21578/ping> 有反应吗？
   - 有 → 是插件侧问题，检查 N.E.K.O 的插件是否启用
   - 没有 → 是模组没加载，检查上面的路径

**Q：猫娘看不到游戏画面？**

N.E.K.O 需要配置支持视觉的模型。到 **N.E.K.O 设置 → API → 视觉聊天模型** 填好即可。
没配的话她会告诉你「看不到画面」，而不是瞎描述。

**Q：她点按钮点不动？**

界面上有些按钮是**灰的**（比如没选中存档时的「Play」），她看不到也点不了 ——
这是有意过滤的。让她先点一下存档卡片，那些按钮才会出现。

游戏切界面**比较慢**，她点完会自动等约 3 秒再确认。如果一直没变化，
让她重新列一遍界面元素。

**Q：造火箭时零件没接上/发射就散架？**

用「载入蓝图」，别手拼。见第六节。

**Q：独占模式开了之后我鼠标动不了？**

配置页把开关关掉，或按 **F10**。

**Q：改了端口之后插件连不上？**

端口要**改两处**：模组侧的 `sfs-agent.ini` 和插件侧的 `plugin.toml`
（`bridge_url`）。只改一处两边就对不上了。

好消息是模组侧改端口**不用重启游戏** —— 保存配置后会立刻在新端口上重开监听。

**Q：想让她别老说话 / 更主动一点？**

这属于 N.E.K.O 宿主侧的人设和主动发言设置，不在本插件的管辖范围内。

---

## 九、给折腾党的备注

### 配置在哪

- **模组侧**：`Mods\SFS-Agent\sfs-agent.ini`，或直接打开
  <http://127.0.0.1:21578/> 在页面上改（改完**不用重启**，端口除外——
  端口会自动重开监听，也不用重启）
- **插件侧**：`plugin.toml` 的 `[sfs_bridge]` 段

### 模组还能干什么

它不只是给猫娘用的。任何能访问本机的程序都能接入：

```python
import requests
B = "http://127.0.0.1:21578"

print(requests.get(f"{B}/state").json())          # 遥测
ui = requests.get(f"{B}/ui").json()               # 界面上有啥可点的
requests.post(f"{B}/ui_click", json={"index": 4}) # 点第 4 个
requests.post(f"{B}/key", json={"vk": 32})        # 按空格（点火）
requests.post(f"{B}/command",
              json={"name": "set_throttle", "value": 1.0})
```

完整接口清单、返回示例、踩过的坑，都在
[SFS-Agent 仓库](https://github.com/LShangPiao/SFS-Agent) 的 README 里。

### 模组能改游戏设置

除了给猫娘用，也能程序化改游戏的音量、画面、帧率等：

```bash
curl http://127.0.0.1:21578/settings                     # 看全部可改项
curl -X POST -H "Content-Type: application/json" \
     -d '{"key":"fps","value":60}' http://127.0.0.1:21578/settings
```

可改的有：音效/音乐音量、界面缩放与透明度、镜头抖动、FXAA 抗锯齿、
轨道线数量、帧率上限、垂直同步、窗口模式。

---

## 十、关于我们

由 **星河拓航Studio**（Galaxy Exploration Studio）开发与维护 ——
一个由来自五湖四海的航天爱好者组成的非正式线上航天科普组织。

- 官方网站：<https://xhth.top/>
- B 站主页：<https://space.bilibili.com/3546949529635067>

欢迎反馈问题与建议。
