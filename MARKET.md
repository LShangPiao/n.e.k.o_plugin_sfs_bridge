# 和猫娘一起造火箭(SFS) · Market 投稿材料

本文件是**投稿用**的文案与说明，不参与插件运行，也不会被打包进
`.neko-plugin`。日常使用请看 [README.md](README.md)。

---

## 一句话简介

> **让猫娘陪你一起玩《航天模拟器》：她能看懂你的火箭、读飞行遥测、操作游戏界面，
> 还能直接把整枚设计好的火箭加载进建造台。**

## 详细介绍

**猫娘终于能进《Spaceflight Simulator》了。**

她会读仪表的：高度、速度、油门、当前分级、总质量、是否可控，随口就能报给你。
你说「我这火箭怎么样」，她会调出零件清单和画面一起分析，而不是空口夸两句。

她也会动手：**点主菜单、载入存档、开关设置、在建造台操作**，全程走游戏内事件注入
——**你的鼠标不会被抢走，游戏窗口也不用切到前台**，你可以一边用电脑做别的事，
一边看她玩。

**造火箭**是最省事的一环：她能把你自己存过的设计**整枚加载**进建造台
（尺寸、纹理、分级全部由游戏自己解析，所以一定是正确、能飞的），
也能按你说的在指定坐标补一个零件。

想让她自己上手飞也行：点火、推油门、分级、开 RCS、转向，都能通过对话下指令。

**还有一个「造火箭控制台」面板**：不用开口，直接在插件页面里看遥测、抓画面、
点游戏按钮、按住键转向、载入蓝图、改配置。面板和她调的是同一批能力，
两边不会打架 —— 你也可以一半自己操作、一半让她接手。

### 亮点

- 🎯 **不抢鼠标、不抢焦点** —— 点击走游戏自己的输入派发，按键靠拦截
  `UnityEngine.Input`。你在前台干别的，她在后台照样玩
- 🚀 **整枚蓝图加载** —— 不是手搓零件，是把游戏自己的蓝图交给游戏自己解析，
  实测加载 141 零件、712.4 吨的火箭，数据与蓝图完全一致
- 📊 **有数就说数** —— 遥测、零件构成、质量、推重比都直接来自游戏内存
- 🖥️ **自带控制台面板** —— 六个页签（总览/画面/界面/飞行/建造/设置），
  不对话也能操作；设置改完立即生效，不用重启插件
- 🙅 **读不到就说读不到** —— 见下方「不会瞎编」
- ⚙️ **随包装好的模组** —— 配套的 SFS-Agent 模组开源、可自行编译，
  还自带一个配置页（亮/暗双主题、中英切换）

### 需要先装一个游戏模组

本插件通过一个**游戏内模组**和游戏通信，装法就一步：

1. 从 <https://github.com/LShangPiao/SFS-Agent/releases> 下载 `SFS-Agent.dll`
2. 放进 `Mods\SFS-Agent\SFS-Agent.dll`
3. 重启游戏

没装也能启用插件，只是会如实告诉你「连不上游戏」，不会假装能玩。

### 关于「不会瞎编」

航天模拟器里的数据如果读不到，插件**不会**让模型凭空回答：

- 连不上游戏 → 明确返回「游戏可能没有运行，请如实告诉用户，不要编造飞行数据」
- 抓不到画面 → 明确返回「画面不可用，不要猜测画面内容」

这条对玩航天游戏尤其重要 —— 编出来的轨道参数和零件构成看着像真的，但会误导人。

---

## 建议的 Market 元数据

下面是建议填入 Market 的字段（与 `plugin.toml` 保持一致）：

| 字段 | 建议值 |
| --- | --- |
| 名称 | 和猫娘一起造火箭(SFS) |
| 一句话 | 让猫娘陪你玩《航天模拟器》：读懂火箭、操作界面、加载蓝图、还能帮你飞 |
| 分类 | 游戏 |
| 关键词 | 航天模拟器 SFS 火箭 造火箭 发射 遥测 看画面 界面操作 游戏 模拟器 |

`plugin.toml` 里对应的字段：

```toml
description = "连接 Spaceflight Simulator（航天模拟器），让猫娘读取飞行遥测、查看游戏画面、操作游戏界面（主菜单、载入存档、建造菜单、设置），并能把整枚蓝图直接加载进建造台、对火箭设计给出建议。点击是游戏内输入派发，不会移动你的鼠标。需要先在游戏 Mods 目录安装配套的 SFS-Agent 模组。"
short_description = "让猫娘陪你玩航天模拟器：读懂火箭、操作界面、加载蓝图、还能帮你飞"
```

---

## 面向审核的说明

### 这个插件为什么依赖外部模组

N.E.K.O. 插件跑在 Python 进程里，而《Spaceflight Simulator》是 Unity 游戏 ——
两者之间必须有一个**游戏内**的桥。这是架构决定的，不是偷懒：

- Python 侧读不到游戏内存、也调不到 Unity API
- 游戏侧需要一个 C# 模组来接住请求

所以本插件拆成两层：

| | 本插件（Market 上架的） | SFS-Agent（配套模组） |
| --- | --- | --- |
| 形态 | N.E.K.O. Python 插件 | 游戏内 C# DLL |
| 职责 | 给猫娘的工具、提示词、防幻觉引导 | 遥测 / 截图 / 输入 / 建造 |
| 通信 | ← HTTP `127.0.0.1:21578` → | 只监听回环地址 |

**模组完全开源**：<https://github.com/LShangPiao/SFS-Agent>（GPL-3.0），
代码可自行审阅与编译。它不修改存档、不联网，只做只读遥测加少量控制指令。

### 没有游戏时会发生什么

**审核环境没有《航天模拟器》也能验证本插件是干净的**：

1. 启用插件 —— 正常启动，不报错、不崩溃
2. 调任意工具（如 `get_sfs_status`）—— 返回明确的错误信封，
   文案是「连不上游戏桥接服务。请确认：1) 游戏正在运行；2) 已安装 SFS-Agent
   模组；3) 已重启过游戏」
3. 模型据此如实转述，**不会编造任何飞行数据**

也就是说：**缺少游戏和模组时，插件的表现是「如实说连不上」，而不是异常、崩溃，
或者编数据。**

### 技术说明（可选）

- 通信只走 `127.0.0.1` 回环地址，端口 `21578`，可用 `bridge_url` 配置项更改
- 截图走 N.E.K.O. 自身配置的多模态模型，插件不内置任何模型调用
- 有 `tests/test_smoke.py`（8 个用例），CI 会跑
  `ruff` + `neko-plugin check --release`
- 依赖仅 `httpx` / `pillow`（`pillow` 用于截图压缩）

---

## 投稿检查清单

- [x] 仓库名 `n.e.k.o_plugin_sfs_bridge`（符合 `n.e.k.o_plugin_<id>` 规范）
- [x] `plugin.toml` 字段齐全（id / name / description / short_description /
      version / entry / type / keywords / author）
- [x] `README.md` / `LICENSE`（GPL-3.0）/ `__init__.py` / `pyproject.toml`
- [x] CI 通过，且仓库内**只有一组** `market-evidence` artifact
- [x] `neko-plugin check --release` 通过（`payload_hash_verified=True`）
- [x] 配套模组已发 Release，README 里的下载链接有效
